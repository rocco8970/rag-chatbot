streamlit>=1.10.0,<2.0.0
openai>=1.0.0,<2.0.0
boto3>=1.26.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0
python-dotenv>=0.21.0
pdfplumber>=0.7.4,<1.0.0
python-docx>=0.8.11
pgvector>=0.1.0,<1.0.0
pytest>=7.0.0
pytest-cov>=4.0.0

# Notes:
# - Ranges (>=,<) give stability while allowing security patches within the same major.
# - For strict reproducibility in production, replace ranges with exact pinned versions
#   (e.g. "package==1.2.3") and consider using a lockfile (pip-tools, poetry, or pip freeze).
